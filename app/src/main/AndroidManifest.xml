<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cashcontrol">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CashControl"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Registration1"></activity>
        <activity android:name=".GraphOthers" />
        <activity android:name=".GraphTravel" />
        <activity android:name=".GraphFood" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NotificationView"
            android:label="@string/notification_activity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".LocationShow" />
        <activity android:name=".Predict" /> <!-- <activity android:name=".UserListActivity"></activity> -->
        <activity
            android:name=".Registration"
            android:label="Registration" />
        <activity android:name=".HistoryShow" /> <!-- <activity android:name=".History" /> -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>